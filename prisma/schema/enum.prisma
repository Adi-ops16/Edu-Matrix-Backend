enum Providers {
    GOOGLE
    CREDENTIALS
}

enum Role {
    SUPER_ADMIN
    INSTITUTION_ADMIN
    STUDENT
    TEACHER
}

enum UserStatus {
    ACTIVE
    BAN
}

enum InstitutionStatus {
    PENDING
    APPROVED
    REJECTED
}

enum Gender {
    MALE
    FEMALE
    KINDER
    OTHER
}

enum JoiningStatus {
    PENDING
    APPROVED
    DECLINED
}

enum MemberStatus {
    NOT_JOINED
    PENDING
    APPROVED
    DECLINED
}

enum CourseStatus {
    UPCOMING
    ONGOING
    COMPLETED
}

enum PaymentStatus {
    PENDING
    SUCCESS
    FAILED
    REFUNDED
}

enum PaymentGateway {
    STRIPE
    SSLCOMMERZ
    BKASH
}
