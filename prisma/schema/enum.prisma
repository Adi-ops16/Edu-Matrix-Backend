enum Providers {
    GOOGLE
    CREDENTIALS
}

enum Role {
    SUPER_ADMIN
    INSTITUTION_ADMIN
    STUDENT
    TEACHER
}

enum UserStatus {
    ACTIVE
    BAN
}

enum InstitutionStatus {
    PENDING
    APPROVED
    REJECTED
}
